export {};
//import { Configration } from '../../models/index.js';
/*
export async function AccountsRoutes(app:express.Application) {
    return dbStore['account'] ? await Promise.resolve(await Configration.createRouteInstance(app,'account', await DefaultController.createInstance('account'),
    (self:IDefaultRoutesConfig)=>{
        self.defaultRoutes();
        self.param()
})): console.log('Account model is not avaliable in dbStore No accounts routes configuered');
}
*/ 
